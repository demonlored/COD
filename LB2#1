#include<stdio.h>

int main()
{
    int N,V,v=0,d;
    scanf("%d",&N);
    printf("Ввод : %d \n",N);
    int n = N;
    while(N>0)
    {
        v = v*10+1;
        N = N / 10;
    }
     V=n-v;
    printf("Вывод:  %d",V);
    return 0;
}

