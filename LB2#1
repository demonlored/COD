#include<stdio.h>

int main()
{
    int N,V,v;
    scanf("%d",&N);
    printf("Ввод : %d \n",N);
    while(N>0)
    {
        v = N % 10;
        V = V*10+v-1;
        N = N / 10;
    }
    printf("Вывод:  %d",V);
    return 0;
}
